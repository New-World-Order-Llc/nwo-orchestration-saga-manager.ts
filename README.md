# nwo-orchestration-saga-manager.ts
Manages deterministic multi‑entity sagas. Validates saga envelopes, executes reproducible steps, applies compensating actions on failure, and emits immutable saga packets. Guarantees nondestructive, audit‑aligned long‑running process coordination.
